version: '3.8'

services:
  stream-resolver:
    build: .
    container_name: oxypace-stream-resolver
    restart: always
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - NODE_ENV=production
      - LOG_LEVEL=info
      - ALLOWED_ORIGINS=*
