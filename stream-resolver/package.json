{
  "name": "stream-resolver",
  "version": "1.0.0",
  "description": "HLS/MP4 stream URL resolver — Oxypace entegrasyonu için",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install:browsers": "npx playwright install chromium",
    "setup": "npm install && npx playwright install chromium"
  },
  "keywords": [
    "hls",
    "m3u8",
    "stream",
    "playwright",
    "express"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "playwright": "^1.40.0",
    "playwright-extra": "^4.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
