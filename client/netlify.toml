# Netlify Configuration for Oxypace SPA
# Replaces vercel.json — SPA fallback, security headers, build settings

[build]
  # Build command
  command = "npm run build"
  # Output directory (Vite default)
  publish = "dist"
  edge_functions = "netlify/edge-functions"

# ── Edge Functions ───────────────────────────────
[[edge_functions]]
  path = "/*"
  function = "access-control"

# ── Proxies (ported from vercel.json) ────────────
[[redirects]]
  from = "/api/*"
  to = "https://unlikely-rosamond-oxypace-e695aebb.koyeb.app/api/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/r2-media/*"
  to = "https://pub-094a78010abf4ebf9726834268946cb8.r2.dev/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/socket.io/*"
  to = "https://unlikely-rosamond-oxypace-e695aebb.koyeb.app/socket.io/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/sitemap.xml"
  to = "https://unlikely-rosamond-oxypace-e695aebb.koyeb.app/sitemap.xml"
  status = 200
  force = true

# ── OG / Bot Proxy (WhatsApp, Telegram, Facebook Preview) ─────────────────────
# WhatsApp ve diğer mesajlaşma botları JS çalıştırmaz; sadece OG meta etiketlerini okur.
# Bot'lar /post/:id, /profile/:username, /portal/:id gibi URL'lere istek attığında
# Koyeb backend'indeki /og route'una yönlendirilir. Bu kurallar SPA fallback'ten ÖNCE gelir.
[[redirects]]
  from = "/post/*"
  to = "https://unlikely-rosamond-oxypace-e695aebb.koyeb.app/og/post/:splat"
  status = 200
  force = true
  conditions = {Role = ["bot"]}

[[redirects]]
  from = "/profile/*"
  to = "https://unlikely-rosamond-oxypace-e695aebb.koyeb.app/og/profile/:splat"
  status = 200
  force = true
  conditions = {Role = ["bot"]}

[[redirects]]
  from = "/portal/*"
  to = "https://unlikely-rosamond-oxypace-e695aebb.koyeb.app/og/portal/:splat"
  status = 200
  force = true
  conditions = {Role = ["bot"]}

# ── SPA Fallback ─────────────────────────────────
# All routes that don't match a static file → serve index.html
# force = false ensures existing static files (.webp, .png, etc.) are served directly
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false

# ── Security Headers (ported from vercel.json) ───
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "DENY"
    Referrer-Policy = "strict-origin-when-cross-origin"
    # Permissions-Policy removed to allow default browser behavior
    X-DNS-Prefetch-Control = "off"

# ── Cache Control for Static Assets ──────────────
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "*.js"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "*.css"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
